# The only credential this project needs. Everything — image models and the text model
# used for style brainstorming — runs through one Replicate account.
#
# Real value lives in td-sops:
#   sops -d ~/projects/td-sops/apps/mwk-og-image-generator.enc.env
REPLICATE_API_TOKEN=r8_...
